# GoalZendo Runpod deployment bundle

This directory is an isolated transport layer for reproducible GPU execution.
The base64 archive is generated from the GoalZendo-only source, configurations,
tests, and launchers in the local research workspace. It does not include
ForkWorld artifacts, checkpoints, credentials, or the unrelated dirty working
tree. The branch is not a paper release and is not merged into main.
